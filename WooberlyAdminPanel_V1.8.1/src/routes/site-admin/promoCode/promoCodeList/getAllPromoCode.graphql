query($currentPage: Int) {
    getPromoCodes(currentPage: $currentPage) {
        status
        errorMessage
        count
        data {
          id
          title
          description
          code
          type
          promoValue
          currency
          expiryDate
          isEnable
          createdAt
          updatedAt
        }
    }
}